// Game Configuration
const GameConfig = {
    // Pickup spawn distances (in pixels)
    magazineSpawnDistance: 1000,  // Spawn magazine every ~300 pixels
    heartSpawnDistance: 2000,     // Spawn heart every ~500 pixels

    // Spawn variance (adds randomness to spawn distances)
    spawnVariance: 500,          // Random offset: -100 to +100 pixels
};
