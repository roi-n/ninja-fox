// Game Configuration
const GameConfig = {
    // Pickup spawn distances (in pixels)
    magazineSpawnDistance: 3000,  // Spawn magazine every ~300 pixels
    heartSpawnDistance: 5000,     // Spawn heart every ~500 pixels

    // Spawn variance (adds randomness to spawn distances)
    spawnVariance: 1000,          // Random offset: -100 to +100 pixels
};
